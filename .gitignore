################################################################################
# Repo-root ignores
################################################################################

# Transient AI/code-review session artifacts (do not commit)
CODE_REVIEW_*_FINDINGS.md

# Claude per-developer local settings (user-specific, do not share)
.claude/settings.local.json

# BMAD workflow artifacts (dev/planning only — not for production branches)
_bmad-output/

# pytest-cov generated JSON report (artefact de build, ne pas commiter)
idp-portal/django_backend/coverage.json

