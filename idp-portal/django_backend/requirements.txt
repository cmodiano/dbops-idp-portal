# Django Backend - Migration from FastAPI
# Python 3.12+ required

# Core Django
Django>=5.1.0,<6.0
djangorestframework>=3.15.0
django-cors-headers>=4.3.0

# Oracle Database Driver
# Using oracledb (successor to cx_Oracle) - Thin mode compatible
oracledb>=3.4.1

# Environment variables
python-dotenv>=1.0.0

# YAML support (for profile import/export)
PyYAML>=6.0.0

# SAML authentication (same as FastAPI for parity)
python3-saml>=1.16.0

# JWT token generation/validation (same as FastAPI for parity)
python-jose[cryptography]>=3.3.0

# Structured logging (same as FastAPI for parity - Story M.8)
structlog>=24.1.0

# HTTP client for health check external services (Story M.8)
requests>=2.31.0

# Development dependencies
pytest>=8.0.0
pytest-django>=4.8.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0  # Code coverage reporting
httpx>=0.27.0
ruff>=0.8.0
mypy>=1.10.0
pytest-mock>=3.15.0

# Test Factories & Data Generation (Story M.9)
factory-boy>=3.3.0
Faker>=26.0.0

# Performance Testing (Story M.9)
pytest-benchmark>=4.0.0

# Code Coverage (Story M.9)
coverage[toml]>=7.6.0
